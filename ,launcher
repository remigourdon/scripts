#!/bin/bash
# Launcher for payloads

FZF_CMD=("fzf" "--delimiter" "/" "--with-nth" "-1")

LAUNCHER_DIR="$( dirname "${BASH_SOURCE[0]}" )"
PAYLOADS_DIR="${LAUNCHER_DIR}/payloads"

SELECTED="$(find "${PAYLOADS_DIR}" -type f ! -name "README.md" | "${FZF_CMD[@]}")"
eval "${SELECTED}"
